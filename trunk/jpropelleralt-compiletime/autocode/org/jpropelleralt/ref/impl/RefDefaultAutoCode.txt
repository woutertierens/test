	/**
	 * Create a {@link RefDefault} for
	 * CRUDE_TYPE values, in the universe
	 * given by {@link Multiverse#getCurrentUniverse()}.
	 * @param value		Initial value 
	 * @return			A new RefDefault
	 */
	public static RefDefault<CRUDE_TYPE> create(CRUDE_TYPE value) {
		return create(CRUDE_TYPE.class, value);
	}

	/**
	 * Create a {@link RefDefault} for
	 * CRUDE_TYPE values.
	 * @param universe		The {@link Universe} we will belong to
	 * @param value			Initial value 
	 * @return				A new RefDefault
	 */
	public static RefDefault<CRUDE_TYPE> create(Universe universe, CRUDE_TYPE value) {
		return create(universe, CRUDE_TYPE.class, value);
	}